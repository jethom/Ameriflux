Install anaconda python
using apt-get
install curl
install inotify-tools
install screen
install subversion
install openssh-serve

install pyinotify tools

copy of svn archive:
svn co https://svn.ssec.wisc.edu/repos/Ameriflux/trunk/WillowCreek

set time to UTC
sudo dpkg-reconfigure tzdata

install loggernet linux

set up cronjobs 
   cron configuration in svn config directory

When in the field set up Loggernet (saved WillowCreekLoggernet config in config file)
